# Analysis-of-comments-LLM-as-a-Judge-Classification
This project demonstrates the use of an LLM as a judge for text classification. A small dataset with positive and negative examples was used to validate the model’s understanding of structured prompts. The LLM correctly classified most cases, showing potential for scaling to larger datasets and tasks.
